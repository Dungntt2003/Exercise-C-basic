#include<stdio.h>

int main(){
	char str[100];
	printf("Hay nhap chuoi nao : ");
	scanf("%s",str);
	int b[100];
	int i,j,k;
	for (i=0;i<100) b[i]=0;
	for (i=0;i<strlen(str);i++){
		for (k=0;k<n;k++){
			if (str[i]==)
		}
}
